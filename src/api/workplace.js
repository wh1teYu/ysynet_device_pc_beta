import {_local} from './local';


const workplace = {
    commissionList: `${_local}/remindController/commissionList`,        //待办事项
    queryBillList: `${_local}/remindController/queryBillList`,                  //最新单据列表
    billTypeList: `${_local}/remindController/billTypeList`,                //单据类型列表
}

export default workplace;
